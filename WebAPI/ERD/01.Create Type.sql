USE MASTER
GO

USE MASTER
GO
DECLARE @SQL VARCHAR(1000)=''
SELECT @SQL+=CONCAT('Kill ', SPID, ';') FROM SYSPROCESSES WHERE DBID=DB_ID('TRIPBOX')
EXECUTE(@SQL)
GO

DROP DATABASE TRIPBOX
GO

CREATE DATABASE TripBox
GO

USE TRIPBOX
GO

CREATE TYPE [ID]		FROM BIGINT NOT NULL
GO

CREATE TYPE [FKID]		FROM BIGINT NULL
GO

CREATE TYPE [CD]		FROM CHAR(4) NULL
GO

CREATE TYPE [PRICE]		FROM BIGINT NOT NULL
GO

CREATE TYPE [COST]		FROM BIGINT NOT NULL
GO


CREATE TYPE [AMOUNT]	FROM BIGINT NOT NULL
GO

CREATE TYPE [RATE]		FROM NUMERIC(12,2) NOT NULL
GO

CREATE TYPE [JSON]		FROM VARCHAR(MAX) NULL
GO
